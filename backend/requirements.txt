# Dependencias FIJADAS a versión exacta (2026-09-24).
#
# Antes eran rangos (`>=0.9,<1.0` y parecidos) en 31 de 32 líneas, así que cada
# reconstrucción de la imagen era una lotería. En una sola tarde mordió dos veces:
#   - ruff saltó de 0.15 a 0.16, el conjunto de reglas por defecto creció y
#     aparecieron 917 «errores» sin que cambiara una línea de código;
#   - pgvector/numpy empezaron a devolver `list` donde antes daban ndarray, y
#     tres pruebas de fencing se pusieron rojas solas.
# Ninguna de las dos era un defecto del proyecto, y las dos costaron diagnóstico.
#
# Subir una versión es ahora una decisión visible en el diff, no un efecto
# secundario de reconstruir. `ruff` va a la MISMA versión que fija el CI.

# torch CPU-only: sin esta línea, sentence-transformers arrastra el build CUDA de
# PyTorch (~4.5 GB: nvidia-*, triton) desde PyPI. El contenedor corre en CPU en el
# NAS, así que forzamos las wheels +cpu (sortean por delante de las de PyPI por el
# sufijo de versión local, así que pip las elige sin más cambios).
--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0
gunicorn==23.0.0
orjson==3.12.0

# Database
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
pgvector==0.5.0
alembic==1.20.0

# Task queue
celery[redis]==5.6.3
redis==5.3.1

# Config
pydantic-settings==2.15.0

# Auth / Security
bcrypt==4.3.0
python-jose[cryptography]==3.5.0
slowapi==0.1.10
email-validator==2.3.0

# Scheduler
apscheduler==3.11.3

# AI / Matching
numpy==2.5.3
langdetect==1.0.9
sentence-transformers==3.4.1
groq==0.37.1

# CV Parsing
python-docx==1.2.0
PyMuPDF==1.28.2

# File upload
python-multipart==0.0.32

# HTTP client
httpx==0.28.1

# Scraping
beautifulsoup4==4.15.0
lxml==5.4.0
playwright==1.63.0

# Dev / Testing
pytest==8.4.2
pytest-asyncio==0.26.0
httpx  # also used for TestClient
# Fijado a la MISMA version que CI (.github/workflows/ci.yml). Con un rango,
# cada reconstruccion de la imagen podia traer otro linter: paso el 2026-09-24,
# ruff salto de 0.15 a 0.16, el conjunto de reglas por defecto crecio y
# aparecieron 917 "errores" sin que cambiara una linea de codigo.
ruff==0.15.14
