# jobhunt-core — secreto EXCLUSIVO de redis-core (broker del core).
# Copiar a `.env.core.redis.prod` (QNAP: /share/Public/swissjob/.env.core.redis.prod).
#
# redis-core SOLO recibe esta variable (rev. externa #2): nunca URLs de BD ni
# de Celery. Debe coincidir con la contraseña embebida en CORE_BROKER_URL /
# CORE_RESULT_BACKEND de .env.core.prod.

CORE_REDIS_PASSWORD=CAMBIA_PASSWORD_REDIS
