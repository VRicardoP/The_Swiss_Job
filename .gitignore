# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite
frontend/dist/

# Docker
docker-compose.override.yml

# Environment
.env
.env.local
.env.*.local
.env.prod
.env.production
.env.staging
# Secretos del core (jobhunt_core) — NUNCA commitear los reales.
# Por PREFIJO, no por lista: `.env.core.capture.prod` (el DSN con privilegio
# REPLICATION, que lee TODO el WAL del clúster) no estaba en la enumeración y
# un `git add .` lo habría subido. Una lista se queda corta con el siguiente
# fichero; el prefijo no (auditoría 2026-09-23, A19-05).
.env.core.*
# Permitir los templates (la excepción va DESPUÉS del patrón que los captura)
!.env.prod.example
!.env.core.*.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code y herramientas IA (configuración local, no compartir)
.claude/
.ai/
.zencoder/
.zenflow/

# OS
.DS_Store
Thumbs.db

# Testing
.coverage
htmlcov/
.pytest_cache/

# Ruff
.ruff_cache/

# Capacitor native projects (generated per-developer)
frontend/ios/
frontend/android/

# Alembic
backend/alembic/versions/*.pyc

# Datos personales / CVs de usuarios (no subir al repo)
cv.json
cv.md
ANALISIS_MAESTRO_EMPLEABILIDAD_*.md
